class CustomAllExpensesItemModel {
  final String image, title, sustitle, dNumber;


  const CustomAllExpensesItemModel({
    required this.image,
    required this.title,
    required this.sustitle,
    required this.dNumber,
  });
}
